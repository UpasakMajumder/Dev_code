{% from "macro/svg.nunj" import svg %}

{% macro steps(current, steps) %}

  {% set width=(100 / steps.length) + "%" %}

  <div class="steps">
    {% for step in steps %}
      <div class="steps__item" style="width: {{ width }};">
        {% if(loop.index < current) %}
          <div class="steps__circle previous" data-tooltip-placement="right">
            {{ svg('tick') }}
          </div>
        {% elif(loop.index === current)  %}
          <div class="steps__circle current" data-tooltip-placement="right">
            {{ svg('gear') }}
          </div>
        {% else %}
          <div class="steps__circle" data-tooltip-placement="right"></div>
        {% endif %}
      </div>
    {% endfor %}
    <div class="steps__line"></div>
  </div>
{% endmacro %}
